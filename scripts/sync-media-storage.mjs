// MinIO removed — media is served directly from the filesystem cache (tmp/media-cache).
// This script is kept as a no-op so existing references don't break.
console.log("sync:media-storage: no-op (MinIO removed, using filesystem cache)");
